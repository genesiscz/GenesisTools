# Jev control, features 6–10

Requested 2026-09-18; extend feat/jev-gateway-lab / PR #409. Primary implements; Terra/Luna only read-only exploration.

1. Semantic waits: monotonic shared budget, observation-change interface, bounded 1s fallback, semantic fingerprint, batched state/evidence classification, cancellation/deadline/request caps; virtual event replay and tests.
2. Bounded recovery: native pre-dispatch refusal metadata, local remedy inventory, never repeat uncertain mutation, shared budgets plus recovery cap; assist flag and tests.
3. Resilient workflows: versioned semantic plan, exact binding first, optional Jev rebind to observed candidates, exact values supplied by reference, fresh verification, explicit save for repairs; preserve legacy runner.
4. Chooser escalation: exact / Jev / explicit host handoff only, bounded redacted evidence packet, same permitted candidates and budgets; replay comparison.
5. Visual grounding: native OCR on immutable capture, transforms and pixel revalidation, one-use action, optional explicitly provisioned icon adapter and verified artifact cache; no model downloads during normal AX usage.

Each area gets appropriate targeted tests and a logical commit. Update Control Lab, README, Obsidian specs/status/proof, push and update existing normal PR. Do not merge or run a review loop.

Live folder-demo interruption: actual Jev selected all 28 supplied folder labels. Six below semantic gate are admitted by explicit user-name exact binding only. Whole-window see/act guard refused while chat updated. Selector-based AXPress demonstration uses fresh unique anchored regex/AXRow selectors, reads AXExpanded before/after, one-second timed capture. Keep refusal and final proof; do not call it a successful snapshot-token demonstration.

## Extended improvement pass

User expanded the goal at 2026-09-18 01:02 UTC: finish every feature and spend at least four further hours comparing/improving this stack against the Computer Use capabilities available in Codex. User subsequently fixed the deadline: continue at least until 2026-09-18 05:00 UTC (07:00 Prague). Do useful implementation and runtime work throughout; no time-filling sleep.

Comparison baseline is the installed Computer Use public skill/API (@oai/sky through persistent node_repl), not undocumented internals. Map native capability, SDK convenience, observation freshness, cancellation, clipboard, pixel transforms, performance and app coverage. Build repeatable native fixture benchmarks and retain failures as well as successes. Primary implements. Terra performs a bounded read-only gap analysis. No AppleScript or browser scripting for the native UI proofs.

Jev-only constraint (user, 2026-09-18): control tooling may call no other AI model. Both direct TypeSafe and Gateway routes remain Jev. Remove the uncommitted larger-model adapter (never invoked); escalation produces a packet for the current host/user, never another AI API call. Semantic AI use must be explicitly selected/approved.

Visual clarification: use existing native local OCR; no Python dependency. Omit icon detection unless evidence establishes that Codex Computer Use uses it. The loaded public API exposes AX and screenshots, not an icon detector, so the implementation will use native OCR and observed screenshot regions.

Replacement constraint: no Codex Computer Use/Sky service or internal API dependency. Implement native AX/CoreGraphics/Vision + GenesisTools interfaces. Ordinary npm dependencies are allowed; @oai/sky is a comparison reference only, not an implementation dependency. Standalone CLI/REPL/MCP proof must work without the Codex Computer Use plugin.

Additional replacement gaps to handle in this pass: broaden semantic workflows beyond press/set; inspect/remove implicit osascript/Peekaboo fallback from the new native replacement/recording path (legacy named commands can remain explicit); persistent state revisions and MCP facade; expanded targeted key grammar and native app lifecycle; bound cleanup of owned temporary capture markers/images; measured CPU/count/latency comparisons rather than inference from one fixture run.
