# Jev control, features 6–10

Requested 2026-09-18; extend feat/jev-gateway-lab / PR #409. Primary implements; Terra/Luna only read-only exploration.

1. Semantic waits: monotonic shared budget, observation-change interface, bounded 1s fallback, semantic fingerprint, batched state/evidence classification, cancellation/deadline/request caps; virtual event replay and tests.
2. Bounded recovery: native pre-dispatch refusal metadata, local remedy inventory, never repeat uncertain mutation, shared budgets plus recovery cap; assist flag and tests.
3. Resilient workflows: versioned semantic plan, exact binding first, optional Jev rebind to observed candidates, exact values supplied by reference, fresh verification, explicit save for repairs; preserve legacy runner.
4. Chooser escalation: exact / Jev / explicit host or configured model adapter, bounded redacted evidence packet, same permitted candidates and budgets; replay comparison.
5. Visual grounding: native OCR on immutable capture, transforms and pixel revalidation, one-use action, optional explicitly provisioned icon adapter and verified artifact cache; no model downloads during normal AX usage.

Each area gets appropriate targeted tests and a logical commit. Update Control Lab, README, Obsidian specs/status/proof, push and update existing normal PR. Do not merge or run a review loop.

Live folder-demo interruption: actual Jev selected all 28 supplied folder labels. Six below semantic gate are admitted by explicit user-name exact binding only. Whole-window see/act guard refused while chat updated. Selector-based AXPress demonstration uses fresh unique anchored regex/AXRow selectors, reads AXExpanded before/after, one-second timed capture. Keep refusal and final proof; do not call it a successful snapshot-token demonstration.
