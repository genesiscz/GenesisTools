node_modules
.output
.nitro
.tanstack
.vite
dist

# Generated by TanStack Router
src/routeTree.gen.ts

# Local SQLite DB + WAL sidecars (never commit account data)
*.db
*.db-shm
*.db-wal
data/

# Secrets — env files are local-only; .env.example is the committed template
.env
.env.local
.env.*.local

# Playwright-mcp scratch (verification artifacts, never committed)
.playwright-mcp/
*.png

# Playwright e2e — throwaway test DB, saved auth cookie, and reports (never committed)
.e2e/
tests-e2e/.auth/
test-results/
playwright-report/
.playwright/
