TODO(integrator): add search/editor/number to inquirerBackend

Agent A owns inquirer-backend.ts. This file is a reminder to add the three
new PromptBackend methods (search/editor/number) to that file after Agent A's
migration lands.

Reference implementations:
- search  → @inquirer/search `search({ message, source: async (input) => opts.options(input ?? "") })`
- editor  → @inquirer/editor `editor({ message, default: opts.initialValue, postfix: opts.postfix })`
- number  → @inquirer/number `number({ message, default: opts.initialValue, min: opts.min, max: opts.max, validate: opts.validate })`

The PromptBackend interface is in src/utils/prompts/p/backend.ts.
The type definitions are in src/utils/prompts/p/types.ts (SearchOpts, EditorOpts, NumberOpts).
