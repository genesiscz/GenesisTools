/**
 * Moved to `@ui/components/chart-box` so every dashboard shares one implementation.
 * Kept here as a re-export because shops imports it from a dozen places.
 */
export { ChartBox } from "@ui/components/chart-box";
